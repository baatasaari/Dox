meta {
  name: List Webhooks
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/notify/webhooks
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  tenant_id: {{tenantId}}
}
