meta {
  name: Create User
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/v1/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "tenant_id": "{{tenantId}}",
    "email": "operator@acme.corp",
    "password": "securepass1",
    "role": "operator"
  }
}
