meta {
  name: Change Password
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/v1/users/{{userId}}/change-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "current_password": "securepass1",
    "new_password": "newsecurepass2"
  }
}
