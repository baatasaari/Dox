meta {
  name: List Alerts
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/v1/sentinel/alerts
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  tenant_id: {{tenantId}}
}
