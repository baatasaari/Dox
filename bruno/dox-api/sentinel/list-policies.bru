meta {
  name: List Policies
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/sentinel/policies
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  tenant_id: {{tenantId}}
}
